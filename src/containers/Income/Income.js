import React from 'react';
import MultiSelect from '../../components/MultiSelect';

const Income = ({ select, categories }) => {
  return <MultiSelect select={select} categories={categories} />;
};

export default Income;
